init用于初始化，确定单词表目录
start读取单词表，开始背单词
shell用于人机交互
// 上下箭头用来翻页，左返回上级，右进入
// /back返回，/end结束，
type_in从键盘或文件读取新单词，并导出单词表或添加到多个单词表末尾
->读取空行停止
setting设置背单词参数，cdw候选词数量，men最大错误数，
错误码：-1用户操作错误；-2系统文件错误；1正常重启；0正常退出
先问知不知道，知道就问，错了扣2点，不知道扣一点，并展示正确选项。
建议：

背单词可以通过左右箭头控制level
只有shell负责管理屏幕刷新与选项选择，接受子程序错误码：-1用户中断退出，0正常退出，1需要重启，其他错误码。
setting 改为 config
